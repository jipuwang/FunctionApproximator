#pragma once
#include <vector>
// THis is the function used to 
// todo: change the int elementaryFunction to a enumType
void linearizeCurrent(int elementaryFunctionType, double lb, double ub, 
        int meshPts, aCurrent, bCurrent);
:
